apiVersion: v1
kind: Service
metadata:
  name: flask-service
  namespace: flask-app
spec:
  type: ClusterIP
  selector:
    app: flask
  ports:
    - name: http
      port: 80          # ALB will send traffic here
      targetPort: 5000  # Flask container listens here

